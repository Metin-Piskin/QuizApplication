import { StyleSheet } from "react-native";

export default StyleSheet.create({
    bannerContainer: {
        justifyContent: 'center',
        alignItems: 'center'
      },
      banner: {
        width: 300,
        height: 300,
        resizeMode: 'contain'
      }
});